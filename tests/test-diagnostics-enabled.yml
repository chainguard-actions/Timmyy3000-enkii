name: Test diagnostics enabled
on:
  workflow_dispatch:
jobs:
  test:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@de0fac2e4500dabe0009e67214ff5f5447ce83dd # v6.0.2

      - id: action
        uses: .
        continue-on-error: true
        with:
          openrouter_api_key: "sk-or-fake-key-for-testing-only"
          diagnostics: "true"
          run_security: "false"

      - name: Assert diagnostics outputs when diagnostics enabled
        env:
          OUTCOME: ${{ steps.action.outcome }}
          DIAGNOSTICS_PATH: ${{ steps.action.outputs.diagnostics_path }}
          DIAGNOSTICS_NAME: ${{ steps.action.outputs.diagnostics_name }}
        run: |
          echo "outcome: $OUTCOME"
          echo "diagnostics_path: $DIAGNOSTICS_PATH"
          echo "diagnostics_name: $DIAGNOSTICS_NAME"
          if [ "$OUTCOME" = "success" ]; then
            # When diagnostics=true, diagnostics_path should be set
            if [ -n "$DIAGNOSTICS_PATH" ]; then
              echo "PASS: diagnostics_path is set: '$DIAGNOSTICS_PATH'"
            else
              echo "FAIL: expected diagnostics_path to be set when diagnostics=true"
              exit 1
            fi
          elif [ "$OUTCOME" = "failure" ]; then
            echo "PASS: action failed gracefully (expected on sentinel repo / non-PR event)"
          else
            echo "FAIL: unexpected outcome '$OUTCOME'"
            exit 1
          fi
